import React from 'react'
import ReactDOM from 'react-dom/client'
import App80s from './App80s'
import '../globals.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App80s />
  </React.StrictMode>,
)
